export const NO_SESSION_TOKEN = "NO_SESSION_TOKEN" as const;
export const UNAUTHORIZED = "UNAUTHORIZED" as const;
export const FORBIDDEN = "FORBIDDEN" as const;
export const INVALID_REQUEST = "INVALID_REQUEST" as const;
export const INVALID_CREDENTIALS = "INVALID_CREDENTIALS" as const;
export const CARD_NOT_FOUND = "CARD_NOT_FOUND" as const;
export const SET_TRACKED = "SET_TRACKED" as const;
export const SET_UNTRACKED = "SET_UNTRACKED" as const;
